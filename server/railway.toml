[build]
builder = "NIXPACKS"
buildCommand = "npm run build --workspace=server"

[deploy]
startCommand = "npx prisma migrate deploy --schema=server/prisma/schema.prisma || true && npm run start --workspace=server"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "ronbri-server"
